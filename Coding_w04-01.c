#include <stdio.h>

int main(){     
    printf("Hello,\n"); 
    printf("c Token!\n"); 
    return 0; 
}
// Keyword คือ int, return (คำสงวนที่มีความหมายเฉพาะในภาษา C)
// Identifier คือ main, printf (ตัวระบุชื่อฟังก์ชันหรือตัวแปร)
// Constant คือ "Hello,\n", "c Token!\n", 0 (ค่าคงที่ที่ไม่เปลี่ยนแปลง)
// Operator คือ (), ;, =, # (สัญลักษณ์ที่ใช้ดำเนินการต่าง ๆ)